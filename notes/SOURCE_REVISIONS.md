# Source Revisions

Исходники склонированы напрямую с GitHub 2026-07-28, каталоги `.git` удалены
(проект хранит плоский снимок, как и `../MacOS/`).

| Проект | Локальный путь | Commit исходного снимка |
| --- | --- | --- |
| MacTahoe-kde | `source/MacTahoe-kde` | `4c0ad8f` |
| MacTahoe-gtk-theme | `source/MacTahoe-gtk-theme` | `beeb7a2` |
| MacTahoe-icon-theme | `source/MacTahoe-icon-theme` | `77eebfc` |

Для обновления исходников лучше сравнивать новые файлы с текущим проектом
локально, а не перезаписывать вслепую (особенно если после установки в
`installed/` уже появятся точечные правки поверх апстрима).
