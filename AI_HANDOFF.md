# AI Handoff

Отвечай пользователю только на русском языке.

Этот проект — новый перенос темы, стиль macOS **Tahoe (26)**, на замену/рядом
с существующим проектом `../MacOS/` (тема WhiteSur, стиль более старых macOS —
Big Sur/Monterey). Оба проекта независимы, живут в соседних директориях.

## Откуда взято

Три отдельных апстрим-репозитория одного автора (vinceliuice, тот же автор,
что и WhiteSur):

| Проект | Локально | Апстрим |
| --- | --- | --- |
| MacTahoe-kde | `source/MacTahoe-kde` | github.com/vinceliuice/MacTahoe-kde |
| MacTahoe-gtk-theme | `source/MacTahoe-gtk-theme` | github.com/vinceliuice/MacTahoe-gtk-theme |
| MacTahoe-icon-theme | `source/MacTahoe-icon-theme` | github.com/vinceliuice/MacTahoe-icon-theme (включает курсоры, отдельного репозитория курсоров нет) |

Точные коммиты снимка — `notes/SOURCE_REVISIONS.md`. Клонировано
2026-07-28, `.git` каждого репозитория удалён (проект хранит плоский снимок
исходников, как и `../MacOS/`).

## Активное состояние системы

Тема установлена и применена 2026-07-28 на Plasma 6.7.3 Wayland. Активны:

- Look-and-Feel: `com.github.vinceliuice.MacTahoe-Light`;
- Kvantum: `MacTahoe`;
- цветовая схема: `MacTahoeLight`;
- иконки: `MacTahoe-light`;
- курсоры: `MacTahoe-cursors`;
- GTK: `MacTahoe-Light`;
- SDDM: `MacTahoe-Light`.

Подробный журнал установки и проверки — `notes/CURRENT_STATE.md`.
Откат к WhiteSur возможен через рабочий `../MacOS/install.sh`.

## Важные особенности install-скриптов исходников

Каждый из трёх содержит собственный `install.sh` с независимыми опциями,
проверять `--help` перед использованием, если нужно что-то нестандартное:

- **MacTahoe-kde**: `-c/--color [light|dark]` (по умолчанию — оба).
- **MacTahoe-gtk-theme**: `-c/--color`, `-t/--theme [default|blue|purple|...|all]`
  (акцентный цвет), `-a/--alt [normal|alt|all]` (стиль кнопок окна), `-o/--opacity
  [normal|solid]`, `-b/--blur` (требует GNOME Shell расширение Blur My Shell —
  **на KDE неприменимо**, не включать), `-s/--scheme [standard|nord]`.
- **MacTahoe-icon-theme**: `-t/--theme [default|blue|purple|...|nord|all]`
  (по умолчанию — только `blue`, если нужны все цвета — `all`), `-b/--bold`
  (более жирные иконки на панели).

`install.sh` в корне этого проекта — обёртка над всеми тремя, вызывает их с
консервативными дефолтами (не `-b/--blur`, поскольку это GNOME-специфичная
опция). Если пользователь просит другой акцент/цвет — редактировать вызовы
внутри обёртки, а не спорить с апстримными скриптами напрямую.

## Что делать после изменений

По аналогии с `../MacOS/`:

1. Обновлять `notes/CURRENT_STATE.md` после изменения активных вариантов.
2. Обновлять `installed/README.md`, если меняются фактические пути установки.
3. Вести раздел инцидентов в этом файле, если что-то ломается при
   обновлении пакетов KWin/KF6 (в `../MacOS/` уже есть похожий повторяющийся
   инцидент с forceblur — возможно, актуален и здесь, если MacTahoe-kde тоже
   использует кастомный KWin blur-эффект — проверить `source/MacTahoe-kde`
   на наличие C++/QML эффектов, не только Aurorae/Kvantum).

## Инцидент: невидимый курсор (см. `notes/CURRENT_STATE.md`)

2026-07-28 обнаружен и исправлен баг в `source/MacTahoe-icon-theme/cursors/install.sh`:
цикл по всем именам курсоров на каждой итерации сносил и пересобирал
`cursors_scalable`, из-за чего в финальной установке SVG оставался только у
последнего по алфавиту имени (`openhand`). Из-за этого Plasma 6 на Wayland
не могла отрисовать SVG-курсоры (`default` и почти все остальные) и рисовала
битую замену. Скрипт исправлен (копирование базового шаблона вынесено из
цикла), курсоры переустановлены. Подробности и статус проверки — в
`notes/CURRENT_STATE.md`.

## Матовое стекло Dolphin (боковая панель, рамка окна) — 2026-07-28

Пользователь попросил привести цвет боковой панели/рамки/статус-бара Dolphin
к единому виду с основной областью файлов (стиль macOS vibrancy). Сделано по
частям, три независимых механизма:

1. **Боковая панель** — `window.color`/`inactive.window.color` в
   `~/.config/Kvantum/MacTahoe/MacTahoe.kvconfig` (и синхронно в
   `source/MacTahoe-kde/Kvantum/MacTahoe/MacTahoe.kvconfig`) изменены с
   `#f5f5f5`/`#f5f5f574` на `#ffffff`/`#ffffff74`. **Важно:** уже открытые
   окна Dolphin конфиг Kvantum на лету не подхватывают — нужен полный
   перезапуск процесса (`pkill -9 -f dolphin`), не просто закрытие окна
   (Dolphin может держать фоновый процесс/другие окна на старом конфиге).
2. **Статус-бар** — **не поддаётся** правке через Kvantum в принципе:
   `DolphinStatusBar` — кастомный `QWidget`, а не `QStatusBar`, секция
   `[StatusBar]` на него не действует (та же находка независимо
   переоткрыта и подтверждена как в `../MacOS/`, так и здесь). Не тратить
   время на новые правки `[StatusBar]`.
3. **Рамка окна (заголовок, Aurorae)** — два отдельных слоя:
   - Прозрачность: во всех 12 плоских непрозрачных заливках
     `~/.local/share/aurorae/themes/MacTahoe-Light/decoration.svg`
     (6 активных `#ffffff` + 6 неактивных `#f2f2f2`, группы `decoration-*`/
     `decoration-inactive-*`) выставлен `opacity="0.75"`. Синхронизировано в
     `source/MacTahoe-kde/aurorae/MacTahoe-Light/decoration.svg`. Правка на
     диске не подхватывается голым `qdbus6 ... reconfigure` — нужен сброс
     `~/.cache/ksvg-elements` и переключение темы декорации на другую и
     обратно (детали и полный список из 12 путей — история чата, при
     повторной правке искать по `path6092-3`/`path6339-9`/`rect9` и т.п.).
   - Настоящий блюр (а не просто прозрачность): отдельный механизм, см.
     `docs/KWIN_FORCEBLUR_DECORATION.md`. Кратко — оба движка Aurorae,
     доступных в системе (`org.kde.kwin.aurorae` и `.v2`), не регистрируют
     блюр-регион рамки сами; в `source/kwin-forceblur-6.7/` (перенесено и
     расширено из `../MacOS/`) добавлен `forceDecorationBlurRegion()`,
     который считает область рамки напрямую и форсирует её блюр для классов
     окон из `[Effect-blur] WindowClasses` (сейчас `dolphin`). Пересборка/
     проверка — `tools/install-kwin-forceblur.sh` / `tools/check-kwin-forceblur.sh`.
     Численно подтверждено, что это настоящий gaussian-блюр, а не плоская
     прозрачность (2026-07-28) — `docs/KWIN_FORCEBLUR_DECORATION.md`, раздел
     «Статус проверки».
   - **Баг со скруглением углов блюра (исправлен в коде, 2026-07-28):** плоский
     прямоугольный форс-регион блюра не знал о скруглении углов окна, из-за
     чего в обоих верхних углах торчали квадратные тёмные наросты поверх
     кривой. Исправлено вычитанием скруглённого выреза из региона
     (`cutRoundedCorner()` в `blur.cpp`, радиус — константа
     `kForcedCornerRadius = 24`). **Уточнение задним числом:** скругление
     самого окна оказалось встроено в `decoration.svg` (кривые Безье в
     `path1023`/`rect5073-4-9-6`), а не от стороннего KWin-скрипта «Rounded
     corners» из README проекта — этот скрипт не установлен и, похоже, не
     нужен для самой рамки; радиус `24` в патче подобран независимо и просто
     совпал. Подробности — `docs/KWIN_FORCEBLUR_DECORATION.md`, раздел
     «Баг и фикс: острые уголки».
   - **Баг с тенью в углу (2026-07-28, исправлено, подтверждено вживую):**
     отдельно от блюра — сама тень-подложка decoration.svg
     (`path1019`/`rect6520-3-2-6`, встроена в тему изначально) имела прямой
     внешний контур, из-за чего обрывалась резко на границе окна вместо
     плавного затухания. Правка (скругление квадратичной кривой) внесена в
     `~/.local/share/aurorae/themes/MacTahoe-Light/decoration.svg` и
     синхронизирована в `source/`. После перелогина/перезагрузки сессии
     пользователь подтвердил вживую: блюр рамки Dolphin отображается
     корректно, скруглённые углы без тёмной полосы — см.
     `docs/KWIN_FORCEBLUR_DECORATION.md`, разделы про кэш плагина и про эту
     тень.
   - **Важно, обнаружено при отладке 2026-07-28:** `qdbus6 ... unloadEffect
     forceblur` + `loadEffect forceblur` (то, что делает
     `install-kwin-forceblur.sh`) НЕ гарантирует, что `kwin_wayland` реально
     перечитает `.so` с диска в рамках одной сессии — подтверждено
     экспериментально (пересобранный бинарник с новым отладочным логом не
     дал этот лог после unload+load, хотя md5 файла на диске совпадал со
     свежей сборкой). После первой загрузки плагина в сессии последующие
     пересборки могут молча тестировать старую версию. Не доверять
     `isEffectLoaded forceblur == true` как доказательству, что именно
     ПОСЛЕДНЯЯ сборка активна — для гарантии нужен перелогин/перезагрузка.

4. **Полная унификация прозрачности Dolphin (сайдбар/список файлов/панель
   адреса), 2026-07-28** — подробности в `notes/CURRENT_STATE.md`, раздел
   «Dolphin: полная унификация прозрачности». Кратко:
   - `GeneralColors`: `window.color`/`base.color`/`alt.base.color` сведены
     к одинаковой альфе 0.75 для активного и неактивного состояния (было
     непрозрачно/полупрозрачно по-разному).
   - `[%General] no_inactiveness=true` — отдельный флаг Kvantum,
     затемнявший неактивные окна поверх цветов из `GeneralColors`.
   - **`[Hacks] transparent_dolphin_view=true`** — ключевой, официальный
     хак самого Kvantum именно для `KItemListContainer` Dolphin (см.
     исходники стиля, `style/polishing.cpp:452-468`, `Kvantum.cpp:3331`).
     Без него список файлов рисуется с дополнительной непрозрачной
     рамкой/заливкой поверх и не реагирует на блюр вообще, независимо от
     альфы в конфиге. Нашли, читая исходники Kvantum (локальный клон в
     `~/whitesur-build/Kvantum/Kvantum/`), не догадкой.
   - `MacTahoe.svg`, `toolbar-normal*` (6 путей) — сделаны полностью
     прозрачными (`opacity=0`), чтобы не удваивать альфу поверх
     `window.color` (иначе панель адреса светлее сайдбара/списка файлов).
   - Проверено строго: несколько точек с известным фоном (скриншот с
     минимизированным окном), факт сравнивается с формулой
     `0.75×белый + 0.25×фон`, а не визуально на глаз.

5. **Блюр рамки распространён на все окна, не только Dolphin/Alacritty,
   2026-07-28** — `shouldForceBlur()` в `blur.cpp` понимает `*` в
   `WindowClasses` как «форсировать для любого окна». `kwinrc`:
   `WindowClasses=dolphin,alacritty` → `*`. Дефолт в
   `tools/install-kwin-forceblur.sh` тоже стал `*` (переопределить —
   `FORCEBLUR_CLASSES=dolphin,alacritty tools/install-kwin-forceblur.sh`).
   Проверено на System Settings (Kirigami-приложение, не Kvantum/Dolphin) —
   численно совпадает с той же формулой блюра. Подробности —
   `notes/CURRENT_STATE.md`, раздел «Блюр рамки — распространён на ВСЕ
   окна».

**Инцидент, ранее предполагавшийся в разделе выше, подтверждён:** MacTahoe-kde
не имеет собственного C++/QML blur-эффекта, но система (общая для этого и
`../MacOS/` проекта) уже использует кастомный `forceblur.so` вместо штатного
KWin `blur`. Он теперь используется ОБОИМИ проектами одновременно (общий
системный файл `$(qtpaths6 --query QT_INSTALL_PLUGINS)/kwin/effects/plugins/forceblur.so`
и общий `WindowClasses=alacritty,dolphin` в `~/.config/kwinrc`) — при
пересборке из любого из двух проектов актуальными остаются оба класса, они
не конфликтуют, но **не терять `alacritty` из списка**, если правите список
классов из этого проекта. Тот же риск поломки ABI при обновлении `kwin-*`/KF6,
что и в `../MacOS/` — см. `docs/KWIN_FORCEBLUR_DECORATION.md`.

## System Settings: содержимое окна оставалось непрозрачным (рамка — не то же самое), 2026-07-28

Раздел «Блюр рамки — распространён на ВСЕ окна» выше решал только рамку
(Aurorae-декорация), не содержимое окна (сайдбар/страницу KCM) — System
Settings/kcmshell6 создают нативную поверхность окна раньше, чем Kvantum
успевает включить альфа-канал, поэтому Kvantum физически не может сделать
их прозрачными. Рабочее решение — KWin Window Rules (`~/.config/kwinrulesrc`),
принудительная Opacity 75% для классов `systemsettings` и `kcm_*`
(совпадает с формулой Dolphin `0.75×белый + 0.25×фон`); поскольку
`forceblur` уже стоит на `WindowClasses=*`, фон под ней ещё и блюрится.
Подробности, финальный INI и ловушка с обязательным `count=` в
`[General]` (иначе KWin молча стирает правила при `reconfigure`) —
`notes/CURRENT_STATE.md`, раздел «System Settings: содержимое окна
оставалось полностью непрозрачным».

## Прозрачность+блюр Dolphin — распространено глобально на все окна, 2026-07-29

По просьбе пользователя ("хочу чтобы все приложения были как Dolphin, не
настраивать каждое по отдельности") два точечных правила `kwinrulesrc`
(`systemsettings`, `kcm_`) заменены одним универсальным правилом
`wmclassmatch=0` (совпадает с любым классом окна) + четырьмя правилами-
исключениями (`opacityrule=1`/`DontAffect`), которые должны идти в
`rules=` РАНЬШЕ универсального (первое совпавшее правило для свойства
побеждает). Исключены по явному запросу пользователя: браузер (`chrome`),
видео/медиаплееры (`vlc`), просмотр изображений (`gwenview`), IDE
(`jetbrains`, покрывает Android Studio и любые другие JetBrains IDE).
Проверено численно на KCalc (получил прозрачность+блюр, раньше не имел
своего правила) и на Chrome/Gwenview (остались непрозрачными). Полный
INI, обоснование первенства правил и результаты проверки —
`notes/CURRENT_STATE.md`, раздел «Прозрачность+блюр Dolphin распространена
глобально». Если пользователь поставит другой браузер/плеер/редактор — по
той же схеме добавить исключение перед последним правилом.

**2026-07-29, продолжение — воспроизводимость.** Изначально `kwinrulesrc`
был записан вручную (Write-инструментом) и нигде не хранился как скрипт —
в отличие от Kvantum/Aurorae/panel-background, которые уже были
синхронизированы в `source/MacTahoe-kde/` и разворачивались через корневой
`install.sh`. По запросу пользователя «сохрани всё необходимое, чтобы можно
было повторить всё видимое на экране скриптом установки» это исправлено:
- `tools/install-kwin-window-rules.sh` — генерирует и пишет `kwinrulesrc`
  (тот же список исключений по умолчанию: `chrome,vlc,gwenview,jetbrains`,
  переопределяется `TRANSLUCENCY_EXCLUDE_CLASSES`), делает бэкап перед
  записью, соблюдает ловушку `count=`/`rules=`. Проверено: пересоздаёт
  файл побитово идентичным вручную написанному, переживает `reconfigure`.
- `tools/check-kwin-window-rules.sh` — проверка состояния (аналог
  `check-kwin-forceblur.sh`), включая явную проверку рассинхрона `count=`/
  `rules=`.
- `installed/README.md` дополнен полной пронумерованной последовательностью
  воспроизведения текущего визуального состояния с нуля (install.sh →
  курсоры → apply-команды сессии → forceblur → window-rules → SDDM → рестарт
  shell), включая ранее не задокументированную нигде команду фикса имени
  курсора (`kcminputrc`/`gsettings`, апстрим называет курсор
  `MacTahoe-light`, реально устанавливается `MacTahoe-cursors`).
- Выявлено: GTK-тема сессии (`~/.config/gtk-3.0/settings.ini`) на живой
  машине была всё ещё `WhiteSur-Light`, не переключена на MacTahoe —
  `install.sh` разворачивает файлы GTK-темы, но не делает её активной.

## GTK/GNOME-приложения переключены на MacTahoe — 2026-07-29

Пользователь попросил переключить на MacTahoe «полностью всё». Обнаружено,
что KDE-часть (`kdeglobals`, `gsettings org.gnome.desktop.interface
gtk-theme`) уже показывала `MacTahoe-Light`/`MacTahoe-light`, но файлы,
которые GTK-приложения реально читают при старте, — нет:
`~/.config/gtk-3.0/settings.ini`, `~/.config/gtk-4.0/settings.ini`,
`~/.config/xsettingsd/xsettingsd.conf`, легаси `~/.gtkrc-2.0` — все
показывали `WhiteSur-Light` (наследие `../MacOS/`). Добавлен
`tools/apply-gtk-theme.sh` — идемпотентный скрипт, который проставляет
`gtk-theme-name`/`gtk-icon-theme-name`/`gtk-cursor-theme-name` (и
соответствующие ключи xsettingsd/gtkrc-2.0/gsettings, включая
`org.gnome.desktop.wm.preferences theme` для metacity/xfwm-варианта, если он
есть в теме) во всех этих файлах, создавая файлы с нуля, если их нет
(проверено в изолированном `HOME` в песочнице). Прогнан на живой системе —
все четыре файла и три ключа gsettings теперь на `MacTahoe-Light`/
`MacTahoe-light`/`MacTahoe-cursors`. Живых GTK3/4-приложений для визуальной
проверки на этой машине не нашлось (все установленные приложения из числа
проверенных — Qt/Kvantum или собственный рендеринг), поэтому подтверждено
только по содержимому файлов, не скриншотом; `xsettingsd` перечитывает свой
конфиг автоматически при изменении файла (таково его назначение), но уже
запущенным GTK-приложениям всё равно требуется перезапуск, чтобы подхватить
новую тему — это ограничение самого GTK, не скрипта.

## Баг: двойное умножение alpha у Dolphin/Alacritty — 2026-07-29, исправлено

Пользователь заметил на скриншоте, что фон окна Dolphin (список файлов,
сайдбар) заметно темнее рамки/заголовка — стрелками отметил конкретные
области. Численно подтверждено по пикселям скриншота: фон файлового вью
≈RGB(189,216,220), рамка/тайтлбар ≈RGB(209,226,230), разница ~20-25 единиц
на канал устойчиво воспроизводится и в Alacritty (фон терминала
RGB(182,212,216) против рамки RGB(211,228,231)).

Причина — глобальное оконное правило `tools/install-kwin-window-rules.sh`
(добавлено в этой же сессии ранее, коммит `541bbacc`) навешивает
`opacityactive=75` через compositor KWin на ВСЕ окна, кроме списка
`TRANSLUCENCY_EXCLUDE_CLASSES`. В список исключений по умолчанию не попали
`dolphin` и `alacritty` — а оба этих приложения уже имеют СОБСТВЕННУЮ
prea-рендерную альфу фона (Dolphin — `transparent_dolphin_view=true` в
Kvantum kvconfig, красит только `base.color`, текст/иконки остаются
непрозрачными; Alacritty — `opacity=0.75` + `transparent_background_colors=true`
в `alacritty.toml`, альфа только у фоновых ячеек, значение специально
подгонялось под плотность Dolphin, см. комментарий в файле). Compositor-
правило встало ВТОРЫМ слоем поверх уже прозрачного фона → альфа
перемножилась (0.75×0.75≈0.56), контент стал заметно темнее/прозрачнее
рамки.

Рамка не пострадала, потому что `updateDecorationOpacityCompensation()` в
`source/kwin-forceblur-6.7/src/blur.cpp:360-382` (добавлен ранее для
System Settings/KCM) компенсирует эффект оконного правила именно на
`DecorationItem` — отменяет множитель `w->opacity()` на ветке рамки, что
верно для ЛЮБОГО окна, не только KCM. Комментарий в коде на момент
написания прямо говорил «Dolphin needs no such rule» — это предположение
было верным, пока Dolphin не попадал под глобальное правило; новый скрипт
это предположение нарушил, не добавив исключение.

Рассмотрен альтернативный подход — сделать оконное правило единственным
универсальным механизмом для всех приложений (убрав нативную альфу
Kvantum/Alacritty). Отклонён: `opacityactive` в KWin — это compositor-level
множитель поверх УЖЕ отрендеренной текстуры всего окна целиком, включая
текст и иконки, а не только фон. У Dolphin/Alacritty текст/иконки сейчас
остаются полностью непрозрачными благодаря нативной альфе (фон — стекло,
контент — чёткий, как настоящий macOS vibrancy); переход на грубое оконное
правило для этих двух приложений сделал бы текст файлов/терминала тоже
частично прозрачным — заметная деградация, а не упрощение. System
Settings/KCM использует грубое правило не как эталон, а как единственный
доступный вариант (Qt-стиль там не даёт Kvantum достучаться до фона
отдельно от текста).

Фикс: `tools/install-kwin-window-rules.sh` — `TRANSLUCENCY_EXCLUDE_CLASSES`
по умолчанию расширен до `chrome,vlc,gwenview,jetbrains,dolphin,alacritty`.
Правило переустановлено (`tools/install-kwin-window-rules.sh` без
переопределения переменной), `~/.config/kwinrulesrc` теперь содержит 6
правил (5 исключений + общее). Дальнейшая работа (пересборка правила,
визуальная проверка) — см. текущую сессию.

### Продолжение — исключение для Alacritty не сработало (регистр + залипшая opacity), тот же день

После установки фикса пользователь сообщил: фон Alacritty визуально не
изменился. Скриншот + попиксельная проверка подтвердили — фон терминала
всё ещё ≈RGB(184,212,217) против рамки ≈RGB(212,228,231), та же сигнатура
двойного умножения альфы, что и раньше. Причина — ДВЕ независимые проблемы:

1. **Регистр в `wmclass`.** `Rules::matchWMClass` (`rules.cpp:398`, доступен
   локально в `../MacOS/kwin-6.7.3/src/rules.cpp`) сравнивает через
   `cwmclass.contains(wmclass)` — обычный `QString::contains`, РЕГИСТРОЗАВИСИМЫЙ,
   без `Qt::CaseInsensitive`. KDE-приложения регистрируют `resourceClass`
   строчными («dolphin»), поэтому правило `wmclass=dolphin` сработало.
   Alacritty под Wayland регистрирует `app_id`/`resourceClass` как
   `"Alacritty"` (с большой буквы — дефолт winit, `Window::setResourceClass`
   в `window.cpp:178` не приводит регистр). Правило `wmclass=alacritty`
   (строчными) никогда не матчилось. Подтверждено напрямую через временный
   KWin-скрипт (`Scripting.loadScript` + `w.resourceClass`, вывод в
   `journalctl`): `resourceClass=[Alacritty]`. Фикс — писать `Alacritty` с
   большой буквы в `TRANSLUCENCY_EXCLUDE_CLASSES` (см. комментарий в
   `tools/install-kwin-window-rules.sh` рядом с этой переменной).

2. **`evaluateWindowRules()` не сбрасывает opacity, а донакладывает поверх
   текущей.** Даже после исправления регистра и `qdbus6 ... reconfigure`
   ЭТО САМОЕ (уже открытое до фикса) окно Alacritty продолжало показывать
   двойное умножение. Причина в `window.cpp:4430`:
   `setOpacity(rules()->checkOpacityActive(opacity()))` — на вход
   `checkOpacityActive` подаётся ТЕКУЩЕЕ значение `opacity()`, а не
   дефолтная 1.0. Правило типа `DontAffect` (`ForceRule=1`, наше исключение)
   не присваивает `arg` новое значение, а просто прерывает перебор
   (`checkForceStop` возвращает true для любого значения rule != Unused,
   независимо от того, Force это или DontAffect — см.
   `rules.h:363-376`). Значит если opacity окна уже был ошибочно зафорсен
   в 0.75 ДО того, как исключение начало матчиться, `reconfigure` НЕ
   откатывает его обратно к 1.0 — правило-исключение лишь не позволяет
   зафорсить его СНОВА, но не сбрасывает то, что уже было применено раньше.
   Подтверждено вживую: свежеоткрытое окно Alacritty (`setsid -f alacritty`)
   после фикса регистра показывает `opacity=1` (чисто, только нативная
   альфа 0.75 самого приложения); это же (старое) окно этого терминала —
   по-прежнему `opacity=0.75` (зафорсено ранее, залипло).

**Практический вывод:** после любого изменения
`TRANSLUCENCY_EXCLUDE_CLASSES`/переустановки правила уже открытые окна
затронутых приложений нужно закрыть и открыть заново (или перелогиниться) —
`qdbus6 ... reconfigure` не откатывает уже применённый форс opacity для
существующих окон, только предотвращает его на новых.

### Добавлено исключение для Spectacle (скриншоты) — 2026-07-29

Пользователь попросил исключить приложение для скриншотов из глобальной
прозрачности/блюра — полупрозрачное окно снимка экрана мешает работе и
не должно само попадать в собственный скриншот с эффектами. Класс окна
Spectacle проверен вживую тем же способом (временный KWin-скрипт через
`Scripting.loadScript` + чтение `w.resourceClass` из `journalctl`):
`resourceClass=[org.kde.spectacle]` — регистр обычный (строчными), в
отличие от Alacritty проблем с `matchWMClass` тут нет, `wmclass=spectacle`
матчится как есть.

`TRANSLUCENCY_EXCLUDE_CLASSES` по умолчанию расширен до
`chrome,vlc,gwenview,jetbrains,dolphin,Alacritty,spectacle`. Правило
переустановлено, проверено на свежеоткрытом окне Spectacle —
`opacity=1` (прозрачность/блюр больше не применяются).

## Диалоги Dolphin (и любого Kvantum-приложения): фон не совпадал с рамкой — 2026-07-29

Пользователь заметил на «Настройка — Dolphin» (обои временно сделаны белыми
для чистоты сравнения): рамка/сайдбар — `#FFFFFF`, область настроек заметно
серее — `#F6F6F6`, при одинаковой альфе 0.75 (то есть прошлый punch-through
по прозрачности уже работал, проблема была именно в цвете).

Разобрано по исходнику Kvantum (`~/whitesur-build/Kvantum/Kvantum/style/
Kvantum.cpp`, `drawPrimitive(PE_Widget)`, ~строки 1855-1927): для ЛЮБОГО
верхнеуровневого окна Kvantum по умолчанию красит фон SVG-элементом `dialog`
(`[Dialog] interior.element=dialog` в kvconfig) и переключается на `window`
только если первый child в точке (0,0) — `QMenuBar`/`QToolBar` (как у
`QMainWindow` с тулбаром, например у Dolphin). В `MacTahoe.svg`
`dialog-normal`/`dialog-normal-inactive` остались нетронутыми апстримными
`#f6f6f6`/`#f5f5f5` (`fill-opacity:1`, полностью непрозрачные внутри самого
SVG) — их никто не трогал во время унификации прозрачности Dolphin, та
работа шла через `[GeneralColors]`, `decoration.svg` и KWin-правила, а не
через этот отдельный SVG-элемент.

Правка — только цвет (`#f6f6f6`→`#ffffff`, `#f5f5f5`→`#f2f2f2`, в тон
`window-normal`/`window-normal-inactive`), альфа НЕ добавлялась: для
Dialog-окон прозрачность и так форсится KWin-правилом (см. следующий
раздел), добавление альфы в сам SVG повторило бы уже пофикшенный баг
двойного умножения альфы (см. «Баг: двойное умножение alpha» выше).
Синхронно правлено `~/.config/Kvantum/MacTahoe/MacTahoe.svg` и
`source/MacTahoe-kde/Kvantum/MacTahoe/MacTahoe.svg`; бэкап до правки —
`backups/dialog-background_20260729-180306/`. Требует полного перезапуска
процесса приложения (`pkill -9 -f <app>`) — Kvantum не перечитывает SVG на
лету для уже открытых окон.

Поскольку это правка ОДНОГО общего файла темы (не Dolphin-специфичного
кода), фикс системный сам по себе — проверено на диалоге настроек другого,
никак не связанного приложения (KCalc, «Настройка — KCalc», вызван через
`qdbus6 <unique-bus-name> /kcalc/MainWindow_1/actions/options_configure
trigger`) без единой дополнительной правки: `#FFFFFF`/α0.75 везде,
идентично Dolphin.

## Диалоги Dolphin: контент оставался полностью непрозрачным (не только «Настройка») — универсальный фикс через `hastransientparent`, 2026-07-29

После фикса цвета выше пользователь проверил «Свойства» (Alt+Enter/ПКМ на
файле) — фон был полностью непрозрачным (α=1.0), в отличие от рамки
(α=0.75). Причина: punch-through в `tools/install-kwin-window-rules.sh`
матчил ТОЛЬКО заголовок `^(Настройка|Configure) `; под «Свойства ...» он не
подходит, а исключение `wmclass=dolphin` (нужное из-за нативной альфы
главного окна) продолжало ловить и это окно тоже.

Проверено временным KWin-скриптом (`qdbus6 org.kde.KWin /Scripting
loadScript` → `qdbus6 org.kde.KWin /Scripting/Script0
org.kde.kwin.Script.run` → `journalctl --user -b0 -o cat`,
`workspace.windowList()`): окно «Свойства» отдаёт `normalWindow=true,
dialog=false, transient=true`, БЕЗ `windowRole` — отличить его от главного
окна по NET::WindowType или роли нельзя вообще, а по заголовку не
масштабируется на будущие диалоги (Переименовать, «Открыть с помощью» и
т.д.) и локале-зависимо.

Найдено надёжное поле в самом KWin: `hastransientparent`
(`/usr/include/kwin/rulesettings.kcfg`, сравнивается в `rules.cpp:479-480`
как `bool(transientParent) == hastransientparent`) — истинно для ЛЮБОГО
дочернего окна и ложно только для настоящего главного окна, независимо от
заголовка/локали/NET::WindowType.

`tools/install-kwin-window-rules.sh` переписан: punch-through по заголовку
(`DOLPHIN_DIALOG_TITLE_REGEX`) убран целиком; исключение для
`NATIVE_ALPHA_CLASSES` (`dolphin,Alacritty`) теперь скоуплено
`hastransientparent=false`+`hastransientparentmatch=1` (`ExactBoolMatch=1`)
вместо `types=1` — исключается только настоящее главное окно, любое
дочернее автоматически проваливается в общее правило прозрачности без
перечисления заголовков. Механизм автоматически покрывает будущие диалоги
и будущие классы, добавленные в `NATIVE_ALPHA_CLASSES`.

Проверено вживую (полный перезапуск `dolphin` — старые окна не откатывают
уже применённый opacity): «Свойства» — α=0.75/`#FFFFFF`, «Настройка» — без
регрессии, главное окно Dolphin — без двойного умножения альфы (нативные
0.75, не 0.5625).

## Что не делать без явного запроса

- Не запускать `install.sh` (ни обёртку, ни исходников) без подтверждения —
  это меняет вид живой системы.
- Не удалять и не трогать `../MacOS/` — это отдельный рабочий проект,
  пользователь может захотеть вернуться к WhiteSur.
- Не добавлять внешние GitHub-ссылки, кроме апстрима vinceliuice.
